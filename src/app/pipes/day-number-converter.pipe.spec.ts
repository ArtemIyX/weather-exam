import { DayNumberConverterPipe } from './day-number-converter.pipe';

describe('DayNumberConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DayNumberConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
