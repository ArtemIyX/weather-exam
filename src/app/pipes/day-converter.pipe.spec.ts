import { DayConverterPipe } from './day-converter.pipe';

describe('DayConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DayConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
